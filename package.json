{
  "name": "alzgo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:menu": "pnpm --filter menu dev",
    "dev:searchbar": "pnpm --filter searchbar dev",
    "build:menu": "pnpm --filter menu build",
    "build:searchbar": "pnpm --filter searchbar build",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "eslint": "^8.0.0"
  }
}
